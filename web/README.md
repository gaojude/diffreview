# redline-web

Landing page for [Redline](https://github.com/gaojude/redline) — native macOS code review
for the agent era. Next.js + Tailwind, deployed on Vercel.

```sh
npm install
npm run dev
```

The DMG served by the download button lives in `public/`.
